apiVersion: apps/v1
kind: Deployment
metadata:
  name: trend-app-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trend
  template:
    metadata:
      labels:
        app: trend
    spec:
      containers:
      - name: trend
        image: nishajudha/trend-app-project:latest
        ports:
        - containerPort: 80
